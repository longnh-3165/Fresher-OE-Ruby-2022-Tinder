<% provide(:title, t(".title")) %>
<% provide(:button_text, t(".btn")) %>
<div class="container">
  <div class="card mt-5">
    <div class="card-header">
      <%= t ".title" %>
    </div>
    <div class="card-body">
      <%= render "form" %>
    </div>
  </div>
</div>
